---
export interface Props {
    href: string;
    black?: boolean;
}

const { href, black } = Astro.props;

/** All icons via  */
---

<a {href} class:list={['w-[28px]', 'hover:text-accent-400', black ? 'text-black' : 'text-white']}>
    <slot />
</a>
